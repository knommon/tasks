<%= form_for [@task_list, @new_task] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "What to do?" %>
    <%= f.submit 'Add', class: "button" %>
  </div>
<% end %>
